#include <iostream>
#include <cstdlib>


using str = std::string; 
int main() {
    srand(time(0));

    double zeros = 0;
    double ones = 0;
    double twos = 0;
    int x;
    char input;

    do
    {
        x = rand() %3;
        // add logic

        switch (x)  //lol going to do it in python first beacose idk how
        {
        case :
            /* code */
            break;
        
        default:
            break;
        }

    } while (input != 'q' || 'Q');
    




    return 0;
}